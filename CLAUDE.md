# gpui 포크 작업 지침

이 저장소는 `zed-industries/zed`의 포크다. studio 앱(`~/Developments/studio`)이
cargo `[patch]`로 로컬 경로 참조한다. 두 저장소를 함께 봐야 한다.

업스트림 대비 전체 변경 내용과 코드는 **이 저장소 밖**의 `../studio-docs/`에 있다.
git으로 추적하지 않는다.

- 시작점: `../studio-docs/README.md`
- 포크를 수정할 때마다 `../studio-docs/`의 해당 문서를 같이 갱신할 것. 해결된 항목은
  지우지 말고 날짜와 함께 "무엇이 문제였고 어떻게 고쳤는지"로 바꿔 남길 것.

## 규칙

- **새 public API/상태 추가 최소화.** studio 레벨에서 기존 API로 풀 수 있으면 그쪽 우선.
  불가피하면 추가 전에 근거를 설명하고 승인받을 것.
- **새 코드에 설명 주석 금지.** 코드로 표현 못 하는 제약만 주석 허용.
  포크 코드에 남은 주석들은 전부 그 기준을 통과한 것이다(데드락 회피, 동기 호출 이유 등).
- **긴 주석은 쓰지 말 것.** 배경·측정치·대안 검토·"왜 이렇게 됐나"는 전부
  `../studio-docs/`에 쓰고, 코드에는 남기지 않는다. 주석은 **두 줄을 넘기지 않는다.**
  세 줄 이상 쓰고 싶어지면 그건 문서에 갈 내용이다. 코드에 필요한 것은 "여기를 건드리면
  무엇이 깨지는가" 한 문장이지 그 사연이 아니다.
- 포크 수정 시 README "Notice of modifications" 갱신 (Apache-2.0 §4(b)).
- 플랫폼 하나만 구현하고 끝내지 말 것. 최소한 다른 플랫폼의 스텁이 컴파일되는지 확인하고,
  `../studio-docs/`에 어느 플랫폼이 비어 있는지 명시할 것.

## 함정

`../studio-docs/11-pitfalls.md`에 정리되어 있다.
드래그 관련 코드를 건드리기 전에 반드시 읽을 것.
